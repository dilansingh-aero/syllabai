// MySyllabi server config. When these are filled in, the site runs with real
// accounts on Supabase; while empty it runs as a local, in-browser demo.
// The anon key is designed to be public; data is protected by row level security.
window.MYSYLLABI_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
};
